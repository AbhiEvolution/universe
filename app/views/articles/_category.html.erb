<% categories.each do |category| %>
  <span class="fs-6 badge rounded-pill bg-info mt-3 pb-2">
    <%= link_to category.name, category_path(category),class:'text-dark text-decoration-none' %>
  </span>
<% end %>
